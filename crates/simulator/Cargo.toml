[package]
name = "simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.14"
bevy_egui = "0.28"
core_types = { path = "../core_types" }
kinematics = { path = "../kinematics" }
engine = { path = "../engine" }
gcode_parser = { path = "../gcode_parser" }
